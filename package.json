{
  "name": "site-generator",
  "version": "1.0.0",
  "description": "A site generator for vufind",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-uglify": "^3.0.0"
  },
  "devDependencies": {
    "gulp-concat": "^2.6.1"
  }
}
